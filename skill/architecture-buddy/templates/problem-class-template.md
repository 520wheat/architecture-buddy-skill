# 问题类：<一句话命名>

> 产物角色：问题类与边界草稿
> 这不是正式架构设计、ADR 或会议记录；它用于决定后续设计正在解决哪一类问题。

## 何时使用

在正式架构设计开始前，或出现第二个相似变体、需要判断是否应该抽象时使用。它适用于 `pre-development` 和 `reference/retrospective`；后者只记录已有项目事实，不修改被参考项目。

## 问题类与边界

我们正在解决：<用“解决哪一类问题”而不是“建设哪些模块”描述>

核心使用场景：

1. <场景、参与者、输入和期望结果>
2. <场景、参与者、输入和期望结果>

明确不解决：

- <相邻但不属于本问题类的语义>
- <当前阶段明确不做的能力>

必须拒绝的反例：

- <表面相似但不满足同一不变量的问题>
- 拒绝原因：<为什么不能塞进同一个抽象或入口>

## 目标与约束

可量化目标：

- <吞吐/延迟/可用性/一致性/成本/数据量/恢复时间等>

硬约束：

- <预算、兼容性、法规、部署、团队能力或既有契约>

质量属性优先级：

| 质量属性 | 目标或场景 | 对边界/机制的影响 | 代价 |
|----------|------------|------------------|------|
| <属性> | <可观察目标> | <会改变什么> | <引入什么成本> |

## 基本事实与待验证事实

剥掉产品名、框架名和组织惯例后仍成立的事实：

- <基本事实>｜状态：已证实 / 待验证｜证据：<来源或验证方式>

领域不变量：

- <必须保持的状态、顺序、引用、权限或资源边界>

待验证事实及可用默认：

| 事实/策略 | 一期首选默认 | 默认理由 | 触发条件 | 回退路径 | 何时重新打开 |
|-----------|--------------|----------|----------|----------|--------------|
| <未决项> | <默认> | <理由> | <指标/事件> | <回退> | <条件> |

## 机制与策略提示

稳定机制：

- <该类问题几乎绕不开的结构；不要只列模块名>

变化轴与候选策略：

| 变化轴 | 策略选项 | 适用事实 | 代价/风险 | N+1 会改哪里 |
|--------|----------|----------|-----------|---------------|
| <变化轴> | <策略 A/B> | <条件> | <后果> | <新增成本> |

## 下一步进入正式设计的输入

- 已确认的问题类和非目标：<…>
- 需要在架构设计中继续验证的边界：<…>
- 需要写 ADR 的不可逆分叉：<…>
- 需要邀请圆桌的高影响分叉：<…；若无则写“当前未发现”>
- 后续系统架构/详细设计需要承接的契约：<…>

## 修订记录

| 日期 | 变更摘要 | 原因 | 依据 |
|------|----------|------|------|
| <日期> | <变化> | <为什么> | <用户确认/证据/待验证> |
