# 架构设计：<系统或问题名称>

> 产物角色：**正式架构设计文件**
> 场景：pre-development / reference/retrospective
> 状态：draft / design-ready / blocked
> 本文按 ADR-0000 的六个核心板块组织。它是正式设计，不是会议记录；圆桌过程、架构 ADR 和调查证据作为独立附件引用。

## 1. 目标、业务背景与核心约束

### 背景与问题

<系统为谁解决哪一类问题，关键场景是什么。已有项目作为参考时，区分项目事实与本次设计结论。>

### 目标与非目标

- 目标：<可观察的业务或系统结果>
- 指标：<规模、延迟、可用性、一致性、成本或效率目标；未知时标为待验证>
- 非目标：<本阶段明确不解决的相邻问题>

### 约束

- <预算、兼容性、法规、部署环境、团队能力、维护周期或已有契约>

## 2. 实现原则

| 原则 | 要解决的问题 | 代价或边界 |
|---|---|---|
| <原则> | <为什么需要> | <接受的成本、例外或不适用原因> |

<只写真正约束后续设计的原则。不要为了填表罗列通用口号。>

## 3. 逻辑架构视图

### 架构边界与职责

<说明系统与外部参与者的边界，以及核心模块/组件/服务各自拥有和不拥有的责任。模块可以先在同一进程中，不要把部署单元误当作架构边界。>

### 协作与关键路径

<用一条端到端路径说明模块如何通信、数据如何流转、谁拥有状态、失败在哪里被隔离。>

```mermaid
flowchart LR
    A[外部参与者] --> B[入口或适配器]
    B --> C[核心协调模块]
    C --> D[领域/执行模块]
    D --> E[数据或外部系统]
```

### 契约与状态归属

- <关键接口/事件的语义、调用方向、幂等或顺序要求>
- <关键数据和状态由谁拥有，其他模块如何读取或请求变化>

## 4. 技术选型与部署方案

### 选型

| 选择 | 服务的目标或约束 | 放弃的选项及代价 |
|---|---|---|
| <语言、框架、中间件或协议> | <证据、质量属性和理由> | <替代方案与代价> |

### 部署

<说明进程/节点/网络/存储的物理形态。没有必要时明确写单体、单机或“不适用”，不要凭空设计集群。>

## 5. 数据架构与治理

- 数据归属：<哪些模块拥有哪些事实>
- 存储与模型：<只写架构层需要固定的存储类别、核心实体和关系>
- 流转与一致性：<同步/异步、事务边界、读写一致性或回放语义>
- 生命周期：<保留、归档、删除、备份和敏感数据处理；不适用时说明原因>

## 6. 非功能设计

### 高可用、失败与恢复

<根据问题风险说明高可用、容灾、超时、重试、降级、补偿和未知态。分布式调用、状态转换、副作用或安全风险较高时，增加结构化失败表；简单系统不必为了格式制造五列表格。>

| 失败模式 | 可见状态 | 触发与观察 | 恢复/回退 | 残余风险 |
|---|---|---|---|---|
| <仅在确有必要时填写> | <…> | <…> | <…> | <…> |

### 安全与可观测性

<写信任边界、身份/授权、敏感数据处理，以及日志、指标、追踪和告警如何支持验收与排障。>

### 演进

<说明当前切片、下一阶段触发条件、明确不做和如何回退。只有当新增同类需求会改变边界或复杂度时，才补充变化轴、N+1 和反例分析。每个 Milestone 用新记录描述相对上一轮的变化与原因，不覆盖旧记录。>

## 关键决策与验收

### 决策与取舍

<列出最重要的架构选择、被放弃方案、选择理由、代价和重新打开条件；复杂分叉另附架构 ADR。>

### 验收条件

- 刺激：<输入、负载或故障>；可观察响应：<系统状态、用户结果或证据>
- 刺激：<输入、负载或故障>；可观察响应：<系统状态、用户结果或证据>

## 附件与后续交接（按需）

- 圆桌/决策过程记录：<路径；没有触发则写“不适用”>
- 架构 ADR：<路径；没有独立 ADR 则说明由本文承担>
- 调查证据：<路径；只引用与本次决策有关的材料>
- 后续系统架构、详细设计和空骨架需要承接的边界、契约和验收：<…>
